{
  "label": "Secret-like item — never expose the raw value; restricted sensitivity",
  "_note": "The placeholder content avoids embedding even fake-looking real secrets so this fixture is safe to grep through. The point is the sensitivity floor, not the syntactic look of the text.",
  "item": {
    "id": "secret-1",
    "kind": "tool_result",
    "text": "<<TEST_PLACEHOLDER_NEVER_A_REAL_SECRET>>",
    "sensitivity": "restricted",
    "metadata": {
      "secret_like": true,
      "redaction_notes": "treat as opaque token-bearing payload"
    }
  },
  "expected_actions": {
    "floor_public": "filtered",
    "floor_internal": "filtered",
    "floor_confidential": "filtered",
    "floor_restricted": "filtered"
  }
}
